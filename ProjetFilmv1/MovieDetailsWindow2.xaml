<Window x:Class="ProjetFilmv1.MovieDetailsWindow2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Détails du film" Height="700" Width="1000" WindowStartupLocation="CenterOwner" ResizeMode="CanResize" WindowStyle="SingleBorderWindow">
    <Grid Background="White">
        <Border Padding="16" Background="Transparent">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image x:Name="PosterImage" Width="340" Height="510" Stretch="UniformToFill" Margin="0,0,16,0"/>

                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="TitleText" FontSize="26" FontWeight="Bold" TextWrapping="Wrap"/>
                    <TextBlock x:Name="VoteText" Margin="0,8,0,16"/>
                    <ScrollViewer Height="480" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="OverviewText" TextWrapping="Wrap"/>
                    </ScrollViewer>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button x:Name="CloseButton" Content="Fermer" Width="90"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
